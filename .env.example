# Copy this file to .env and adjust if needed (defaults work for local use).

# Port the server listens on
PORT=3000

# Where the SQLite database lives. Default is fine for local use.
DB_PATH=./data/kit-ab.db
